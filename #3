def filter_greater_than(arr, threshold):
    result = []
    for num in arr:
        if num > threshold:
            result.append(num)
    return result

filtered = filter_greater_than([11, 5, 8, 3, 10], 5)
print(filtered)
